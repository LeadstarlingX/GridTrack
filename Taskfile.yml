version: '3'

vars:
  IMAGE_NAME: gridtrack.api
  IMAGE_TAG: latest
  APP_LABEL: gridtrack-api
  COVERAGE_DIR: ./TestResults/Coverage
  REPORT_DIR: ./TestResults/CoverageReport

tasks:
  # ============================================
  # BUILD TASKS
  # ============================================
  
  restore:
    desc: Restore .NET dependencies
    cmds:
      - dotnet restore

  build:
    desc: Build .NET application
    deps: [restore]
    cmds:
      - dotnet build --no-restore --configuration Release


  # ============================================
  # TEST TASKS (no coverage)
  # ============================================

  test-architecture:
    desc: Run architecture tests
    deps: [build]
    cmds:
      - dotnet run --project ./GridTrack.ArchitectureTests/GridTrack.ArchitectureTests.csproj --configuration Release -- --report-trx --report-trx-filename architecture-test-results.trx --results-directory ./TestResults/Architecture

  test-domain:
    desc: Run domain unit tests
    cmds:
      - dotnet run --project ./GridTrack.Domain.UnitTests/GridTrack.Domain.UnitTests.csproj --configuration Release -- --report-trx --report-trx-filename domain-unit-test-results.trx --results-directory ./TestResults/DomainUnit
  
  test-namingconventions:
    desc: Run naming convention tests
    cmds:
      - dotnet run --project ./GridTrack.NamingConventions/GridTrack.NamingConventions.csproj --configuration Release -- --report-trx --report-trx-filename naming-test-results.trx --results-directory ./TestResults/NamingConventions
  
  test-application:
    desc: Run application unit tests
    cmds:
      - dotnet run --project ./GridTrack.Application.UnitTests/GridTrack.Application.UnitTests.csproj --configuration Release -- --report-trx --report-trx-filename application-unit-test-results.trx --results-directory ./TestResults/ApplicationUnit
  
  test-infrastructure-unit:
    desc: Run infrastructure unit tests
    cmds:
      - dotnet run --project ./GridTrack.Infrastructure.UnitTests/GridTrack.Infrastructure.UnitTests.csproj --configuration Release -- --report-trx --report-trx-filename infrastructure-unit-test-results.trx --results-directory ./TestResults/InfrastructureUnit
  
  test-integration:
    desc: Run integration tests
    cmds:
      - dotnet run --project ./GridTrack.IntegrationTests/GridTrack.IntegrationTests.csproj --configuration Release -- --report-trx --report-trx-filename integration-test-results.trx --results-directory ./TestResults/Integration

  test-e2e:
    desc: Run E2E tests (requires Docker + gridtrack-forecasting repo)
    cmds:
      - dotnet run --project ./GridTrack.E2ETests/GridTrack.E2ETests.csproj --configuration Release -- --report-trx --report-trx-filename e2e-test-results.trx --results-directory ./TestResults/E2E

  # ============================================
  # LOAD TEST (k6)
  # ============================================

  k6-run:
    desc: "Single k6 pass against a running stack on :5098. Env: WRITE_BEHIND, QUICK, THRESHOLD_PROFILE, DRIVER_VUS."
    cmds:
      - |
        echo "Waiting for API + seeded drivers on :5098 ..."
        for i in $(seq 1 60); do
        # Fast-fail: container crashed?
        if [ "$(docker compose -f compose.ci.yaml ps -q gridtrack.api | wc -l)" -eq 0 ]; then
        echo "ERROR: API container crashed!"
        docker compose -f compose.ci.yaml logs --tail=50 gridtrack.api
        exit 1
        fi
      
        http_code=$(curl -s -o /dev/null -w "%{http_code}" "http://localhost:5098/health" || true)
        if [ "$http_code" = "200" ]; then
        body=$(curl -sf "http://localhost:5098/api/drivers?pageSize=5" || true)
        if echo "$body" | grep -q '"id"'; then
        echo "  ready"
        break
        fi
        echo "  ($i/60) health OK, seeding..."
        else
        echo "  ($i/60) health=$http_code"
        fi
        sleep 5
        done
      - |
        IDS=$(curl -sf "http://localhost:5098/api/drivers?pageSize=200" \
          | python3 -c "import sys,json; print(','.join(d['id'] for d in json.load(sys.stdin).get('items',[])))")
        k6 run \
          --env BASE=http://localhost:5098 \
          --env DRIVER_VUS=${DRIVER_VUS:-300} \
          --env WRITE_BEHIND=${WRITE_BEHIND:-true} \
          --env QUICK=${QUICK:-true} \
          --env THRESHOLD_PROFILE=${THRESHOLD_PROFILE:-stress} \
          --env TEST_MODE=${TEST_MODE:-latency} \
          --env DRIVER_IDS="$IDS" \
          --env JWT_TOKEN="${K6_JWT_TOKEN}" \
          load-tests/gridtrack.js

  k6:
    desc: "Local convenience: single QUICK write-behind pass (old default behavior)."
    cmds:
      - cmd: WRITE_BEHIND=${WRITE_BEHIND:-true} QUICK=${QUICK:-true} THRESHOLD_PROFILE=${THRESHOLD_PROFILE:-stress} DRIVER_VUS=${DRIVER_VUS:-300} task k6-run

  k6-compare:
    desc: "Write-behind vs direct-postgres comparison (QUICK), FULL reset between arms."
    cmds:
      - cmd: TEST_MODE=latency WRITE_BEHIND=true QUICK=true THRESHOLD_PROFILE=compare DRIVER_VUS=${COMPARE_VUS:-400} task k6-run
        ignore_error: true
      # FULL tear-down, not just Redis flush
      - docker compose -f compose.ci.yaml down -v
      - docker compose -f compose.ci.yaml up -d --build --wait
      - cmd: TEST_MODE=latency WRITE_BEHIND=false QUICK=true THRESHOLD_PROFILE=compare DRIVER_VUS=${COMPARE_VUS:-400} task k6-run
        ignore_error: true
  
  reset-between-arms:
    desc: "Flush Redis, then restart the API container. FORCE_RESEED=true on that restart also wipes+reseeds Postgres."
    cmds:
      - docker compose -f compose.ci.yaml exec -T gridtrack.redis redis-cli FLUSHALL
      - docker compose -f compose.ci.yaml restart gridtrack.api
      - |
        echo "Waiting for API + reseed..."
        for i in $(seq 1 40); do
          if curl -sf "http://localhost:5098/api/drivers?pageSize=5" | grep -q '"id"'; then echo "  ready"; break; fi
          echo "  ($i/40) not ready"; sleep 5
        done

  k6-throughput:
    desc: "Throughput ceiling test — fires telemetry as fast as possible to find the breaking point."
    cmds:
      - cmd: TEST_MODE=throughput WRITE_BEHIND=true QUICK=false THRESHOLD_PROFILE=ceiling DRIVER_VUS=${STRESS_VUS:-480} task k6-run
        ignore_error: true
  
  
  k6-stress:
    desc: "High-VU ceiling test (write-behind only) against a running stack. Override count via STRESS_VUS."
    cmds:
      - cmd: TEST_MODE=latency WRITE_BEHIND=true QUICK=false THRESHOLD_PROFILE=stress DRIVER_VUS=${STRESS_VUS:-480} task k6-run
        ignore_error: true

  k6-ci:
    desc: "CI: bring up compose.ci.yaml, run comparison + latency stress + throughput ceiling, tear down once."
    cmds:
      - docker compose -f compose.ci.yaml up -d --build
      - defer: docker compose -f compose.ci.yaml down -v
      - task: k6-compare
        ignore_error: true
      - docker compose -f compose.ci.yaml down -v
      - docker compose -f compose.ci.yaml up -d --wait
      - task: k6-stress
        ignore_error: true
      - task: k6-throughput
        ignore_error: true


  # ============================================
  # Test ALL TASKS
  # ============================================


  test-all:
    desc: Run all tests
    deps: [ test-architecture, test-domain, test-namingconventions, test-application, test-infrastructure-unit, test-integration, test-e2e ]
    
  test-without-coverage:
    desc: Run all tests without coverage
    deps: [ test-architecture, test-namingconventions ]

  # ============================================
  # COVERAGE TASKS
  # ============================================
  
  coverage-domain:
    desc: Domain unit tests with coverage
    cmds:
      - mkdir -p {{.COVERAGE_DIR}}
      - dotnet-coverage collect
        "dotnet run --project ./GridTrack.Domain.UnitTests/GridTrack.Domain.UnitTests.csproj --configuration Release --no-build"
        -f xml -o {{.COVERAGE_DIR}}/domain.xml
  
  coverage-application:
    desc: Application unit tests with coverage
    cmds:
      - mkdir -p {{.COVERAGE_DIR}}
      - dotnet-coverage collect
        "dotnet run --project ./GridTrack.Application.UnitTests/GridTrack.Application.UnitTests.csproj --configuration Release --no-build"
        -f xml -o {{.COVERAGE_DIR}}/application.xml
  
  coverage-infrastructure:
    desc: Infrastructure unit tests with coverage
    cmds:
      - mkdir -p {{.COVERAGE_DIR}}
      - dotnet-coverage collect
        "dotnet run --project ./GridTrack.Infrastructure.UnitTests/GridTrack.Infrastructure.UnitTests.csproj --configuration Release --no-build"
        -f xml -o {{.COVERAGE_DIR}}/infrastructure.xml
  
  coverage-e2e:
    desc: E2E tests with coverage (requires Docker + forecasting repo)
    cmds:
      - mkdir -p {{.COVERAGE_DIR}}
      - dotnet-coverage collect
        "dotnet run --project ./GridTrack.E2ETests/GridTrack.E2ETests.csproj --configuration Release"
        -f xml -o {{.COVERAGE_DIR}}/e2e.xml
  
  coverage-integration:
    desc: Integration tests with coverage (requires Docker)
    cmds:
      - mkdir -p {{.COVERAGE_DIR}}
      - dotnet-coverage collect
        "dotnet run --project ./GridTrack.IntegrationTests/GridTrack.IntegrationTests.csproj --configuration Release"
        -f xml -o {{.COVERAGE_DIR}}/integration.xml
  
  coverage-report:
    desc: Generate HTML + summary report from all coverage files
    cmds:
      - reportgenerator
        -reports:"{{.COVERAGE_DIR}}/*.xml"
        -targetdir:"{{.REPORT_DIR}}"
        -reporttypes:"Html;Cobertura;MarkdownSummaryGithub"
        -title:"GridTrack Coverage"
        -assemblyfilters:"+GridTrack.*"
  
  coverage:
    desc: Run all tests with coverage then generate report
    cmds:
      - task: coverage-domain
      - task: coverage-application
      - task: coverage-infrastructure
      - task: coverage-integration
      - task: coverage-e2e
      - task: coverage-report


  # ============================================
  # UPDATE README FILE TASKS
  # ============================================

  update-readme-coverage:
    desc: Parse Cobertura.xml and update README.md coverage table
    cmds:
      - python3 update_readme_coverage.py
        
  update-readme-k6:
    desc: Parse k6 JSON results and update README.md load test table
    cmds:
      - python3 update_readme_k6.py

  update-readme:
    desc: Update README with both coverage and k6 results
    deps: [ update-readme-coverage, update-readme-k6 ]

  # ============================================
  # DOCKER COMPOSE TASKS
  # ============================================

  compose-up:
    desc: Start services with Docker Compose
    cmds:
      - docker compose up -d --wait

  compose-down:
    desc: Stop Docker Compose services
    cmds:
      - docker compose down -v

  compose-logs:
    desc: Show Docker Compose logs
    cmds:
      - docker compose logs -f

  # ============================================
  # PIPELINE TASKS (Complete Workflows)
  # ============================================

  ci:
    desc: Run CI pipeline (build + tests)
    cmds:
      - task: build
      - task: test-without-coverage
      - task: coverage
      - task: update-readme-coverage

  cd-compose:
    desc: Deploy with Docker Compose
    cmds:
      - task: compose-down
      - task: compose-up
      - echo "✅ Services started! API available at http://localhost:8000"


  # ============================================
  # UTILITY TASKS
  # ============================================

  clean:
    desc: Clean all resources
    cmds:
      - task: compose-down
      - dotnet clean
      - rm -rf ./TestResults

  help:
    desc: Show available tasks
    cmds:
      - task --list